[pytest]
asyncio_mode = auto
testpaths = tests
# Exclude tests that depend on pydantic-settings env config not available in CI
markers =
    requires_config: tests that need .env or pydantic settings
